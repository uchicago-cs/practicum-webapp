require 'rails_helper'
require 'spec_helper'

describe "Submission pages" do
  # subject { page }

  # before(:each) do
  #   @advisor = User.new(email: "advisor@school.edu",
  #                       advisor: true,
  #                       password: "foobarfoo",
  #                       password_confirmation: "foobarfoo")
  #   @advisor.save
  #   @project = @advisor.projects.build
  #   @project.save
  # end

  # describe "new submission" do
  #   before { visit new_project_submission_path(@project.id) }
  #   # Possibly project with ID 1 does not exist, so we are getting an error
  #   # page.

  #   it { should have_content("Information") }

  #   # describe "create a new submission" do
  #   #   let(:student) { FactoryGirl.create(:student) }
  #   #   before do
  #   #     fill_in 'Information', with: "a" * 100
  #   #   end

  #   #   it "should make our ActionMailer instance send the advisor an email" do
  #   #     expect { click_button "Submit my application" }.to change(ActionMailer::Base.deliveries, :count).by(1)
  #   #   end

  #   # end
  # end
end
