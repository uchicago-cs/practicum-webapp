<h1>New evaluation for <%= true %> <%# @student.email %></h1>

<%= form_for [@submission, @evaluation] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :comments %>
  <%= f.text_area :comments %>

  <%= f.submit "Submit evaluation", class: "btn btn-primary" %>
<% end %>
