<p>Hi <%= @advisor.first_name || @advisor.cnet %>,</p>

<p>A student has applied to your project titled <%= @project.name %>.</p>

<p>Student name: <%= display_name(@student) || @student.cnet %></p>
<p>
  Application: Click <%= link_to "here", submission_url(@submission) %> to view
  <%= @student.first_name %>'s application.
</p>

<p>
  Click <%= link_to "here", project_url(@project) %> to view your project
  information.
</p>
