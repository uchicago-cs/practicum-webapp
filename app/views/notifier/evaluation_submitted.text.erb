Hi <%= @admin.first_name || @admin.cnet %>,

An advisor has submitted an evaluation for a student.

Advisor: <%= display_name(@advisor) || @advisor.cnet || @advisor.email %>
Project: <%= @project.name %>
Student: <%= display_name(@student) || @student.cnet || @advisor.email %>

Visit <%= q_url(@evaluation) %> to view this evaluation.
