Hi <%= @advisor.first_name || @advisor.cnet || @advisor.email %>,

<% if @status == "accepted" %>
Your project proposal for <%= @project.name %> has been accepted. You will be notified when your proposal becomes publicly available and students may apply to it.
<% elsif @status == "pending" %>
Your project proposal for <%= @project.name %> is still pending approval, and we have decided you must make the following changes before it may be approved and before students may apply to it.
<% elsif @status == "rejected" %>
We have decided not to accept your project proposal for <%= @project.name %>.
<% end %>

<% if @comments.present? %>
Administrator feedback:
"<%= @comments %>"
<% end %>

Visit <%= q_url(@project) %> to view <%= "and edit" unless @status == "rejected" %> your project information.
