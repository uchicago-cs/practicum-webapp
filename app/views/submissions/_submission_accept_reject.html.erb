<% if @submission.pending? and before_decision_deadline? %>
  You may
  <%= link_to "accept", accept_submission_path(@submission.id),
                        method: :patch %> or
  <%= link_to "reject", reject_submission_path(@submission.id),
                        method: :patch %>
  this application.
<% end %>
