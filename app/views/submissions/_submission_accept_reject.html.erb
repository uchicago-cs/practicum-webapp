<% if @submission.pending? %>
  You may
  <%= link_to "accept", accept_submission_path(@submission.id) %> or
  <%= link_to "reject", reject_submission_path(@submission.id) %>
  this application.
<% else %>
  You have already <%= @submission.status %> this applicant.
<% end %>
