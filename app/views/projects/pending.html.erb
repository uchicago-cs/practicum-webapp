<h2><%= formatted_current_quarter %> Pending Projects</h2>

<% if @current_pending_projects.count > 0 %>
  <table class="table table-bordered">
    <tr>
      <th>Project title</th>
      <th>Advisor</th>
      <th>Status</th>
    </tr>
    <tbody>
      <% @current_pending_projects.each do |project| %>
	<%= render 'project_short', project: project %>
      <% end %>
    </tbody>
  </table>

  <div>
    Click <%= link_to "here", publish_all_pending_projects_path,
              method: :patch, data: { confirm: "Are you sure?" } %>
    to approve all flagged (non-pending) project proposals.
  </div>

<% else %>
  No projects have been proposed for this quarter.
<% end %>
